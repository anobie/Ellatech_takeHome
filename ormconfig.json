{
  "type": "postgres",
  "host": "db",
  "port": 5432,
  "username": "${DB_USER}",
  "password": "${DB_PASS}",
  "database": "${DB_NAME}",
  "synchronize": false,
  "logging": true,
  "entities": ["dist/**/*.entity.js"],
  "migrations": ["dist/migrations/**/*.js"],
  "cli": {
    "migrationsDir": "src/migrations"
  }
}
